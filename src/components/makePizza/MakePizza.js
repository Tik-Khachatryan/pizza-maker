import React, {useState} from "react";import {Ingredients} from "../ingredients/Ingredients";import {MadePizza} from "../madePizza/MadePizza";import './pizza.css';export const MakePizza = ({saveData, data, createNotification}) => {    const [result, setResult] = useState([]);    function allowDrop(e) {        e.preventDefault();    }    function drop(event) {        event.preventDefault();        const value = event.dataTransfer.getData("text");        setResult(result => [...result, value])    }    return (        <div>            <div className={"heading"}>                <p>Make Your Pizza</p>            </div>            <div className={"makePizza"}>                <div className={"pizza"}                     onDragOver={allowDrop}                     onDrop={drop}>                    <div className={'madePizza'}>                        {                            result.map((item, index) => <img draggable={false} key={index} src={`${item}`}                                                             alt="createdPizza"/>)                        }                    </div>                </div>                <Ingredients/>            </div>            {                result.length > 3 ? <MadePizza saveData={saveData} data={data} createNotification={createNotification}/> : null            }        </div>    )}