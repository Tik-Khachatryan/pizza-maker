import React, {useContext} from "react";import './ingredients.css';import Context from "../../Context";export const Ingredients = () => {    const { handleOnChange } = useContext(Context);    const {ingredients} = useContext(Context)    function drag(event) {        event.dataTransfer.setData("text", event.target.id);    }    return (        <div className={"ingredientsMain"}>            <div className={'head'}>                <p>Ingredients</p>                <table>                    <thead>                    <tr>                        <th>Name</th>                        <th>Image</th>                        <th>Price</th>                        <th>Weight</th>                        <th>Quantity</th>                    </tr>                    </thead>                    {                        ingredients.map(item => {                            return (                                <tbody key={item.name}>                                <tr>                                    <td>                                        {item.name}                                    </td>                                    <td>                                        <img                                            src={`${item.img}`}                                            alt="img"                                            width={'70px'}                                            height={'70px'}                                            draggable={true}                                            onDragStart={drag}                                        />                                    </td>                                    <td>                                        $:{item.price}                                    </td>                                    <td>                                        g:{item.weight}                                    </td>                                    <td>                                        {item.quantity}                                        <input                                            type="number"                                            name={item.name}                                            value={item.quantity}                                            onChange={handleOnChange}                                        />                                    </td>                                </tr>                                </tbody>                            )                        })                    }                </table>            </div>        </div>    )}